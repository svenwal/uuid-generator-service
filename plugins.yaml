_info:
  select_tags:
  - uuid-generator-cicd
plugins:
- name: rate-limiting-advanced
  service: UUID_generator
  config:
    limit:
    - 3
    window_size:
    - 30
    sync_rate: -1
- name: cors
  service: UUID_generator
  config:
    origins:
    - '*'
